'use strict';

/**
 * globale-einstellungen router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::globale-einstellungen.globale-einstellungen');
