'use strict';

/**
 * globale-einstellungen service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::globale-einstellungen.globale-einstellungen');
